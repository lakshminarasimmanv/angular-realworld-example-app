# name: angular-app

# on:
#   push:
#     branches: [ "master" ]

#   pull_request:
#     branches: [ "master" ]

# jobs:
#   docker:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3

#       - name: Setup QEMU
#         uses: docker/setup-qemu-action@v2

#       - name: Setup Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Build and Push
#         uses: docker/build-push-action@v3
#         with:
#           context: .
#           platforms: linux/amd64,linux/arm64
#           push: true
#           tags: hub.narasimman.xyz/angular-app:1.2.3
